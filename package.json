{
  "name": "unified-unit-economics",
  "version": "1.0.0",
  "type": "module",
  "description": "BI as Code Pipeline for Crumble",
  "scripts": {
    "build": "uv venv && uv pip install -r requirements.txt && .venv/bin/python data_gen/generate_data.py && cd dbt_project && ../.venv/bin/python -m dbt deps --profiles-dir . && ../.venv/bin/python -m dbt build --profiles-dir . && cd .. && evidence build",
    "sources": "evidence sources",
    "dev": "evidence dev"
  },
  "dependencies": {
    "@evidence-dev/evidence": "latest",
    "@evidence-dev/duckdb": "latest",
    "@evidence-dev/core-components": "latest"
  }
}
